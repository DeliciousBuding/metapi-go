# STATE.md — Metapi Go product status

**Last verified**: 2026-09-03

> **Current state** (product repository). Only current facts and pointers, no narrative.
> Deployment facts live in the deployment guide; open items → [`progress/MASTER.md`](progress/MASTER.md) · timeline → [`log.md`](log.md) · version narrative → root [`CHANGELOG.md`](../../CHANGELOG.md)

## Current

| Fact               | Value                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| :----------------- | :--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Source             | **[DeliciousBuding/metapi-go](https://github.com/DeliciousBuding/metapi-go)** · default branch `master`                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Latest release     | **[v0.17.0](https://github.com/DeliciousBuding/metapi-go/releases/tag/v0.17.0)** — cut on master 2026-09-03 (tag pipeline + human review of the notes decide publish; until then the **published** Latest remains [v0.16.23](https://github.com/DeliciousBuding/metapi-go/releases/tag/v0.16.23)). v0.17.0 is the data-plane / store honesty wave (#1159–#1170): forwarded-chain client IP resolution, credential echo on account write responses, stream failure accounting with one fallback owner, every table list derived from one schema registry (dialect copy + factory reset), upstream content-encoding honesty, cancellable waits between upstream calls, and rollback-script negotiation by digest. Minor bump under the patch-first cadence because three of them change an operator-visible contract (a factory reset now wipes the whole registry including `admin_sessions`, account write responses no longer carry plaintext credentials, and a broken stream is no longer billed as a success). Sampled release artifact verification and the publish fact are recorded in [`log.md`](log.md) once the tag pipeline lands. |
| Release pipeline   | Single pipeline (`.github/workflows/main.yml`): PR / master push / SemVer tag all run the same 12-check suite; master push pushes the **linux/amd64 + linux/arm64** image (provenance + SBOM, tags latest+sha); SemVer-only tags (`vX.Y.Z`) additionally build **5-platform binaries + checksums.txt** (linux/darwin/windows × amd64/arm64), smoke `metapi-linux-amd64 --version`, and create the GitHub Release **as a draft** (notes extracted from the matching `CHANGELOG.md` section; published after human review of notes); tag must match `web/package.json` version or the release fails |
| Versioning         | `metapi --version` reports the build version injected via `-ldflags -X .../internal/version.Version` (`dev` for local builds); Docker build arg `VERSION`; Makefile `VERSION` variable                                                                                                                                                                                                                                                                                                                                                         |
| Dependency updates | Dependabot active — grouped Go/npm/Actions PRs weekly and Docker PRs monthly through the same 12-check CI; breaking major bumps need a manual migration PR                                                                                                                                                                                                                                                                                                                                                                                     |
| Current focus      | **波次 5（v0.17.0 已切版，2026-09-03）**：备份表清单仍是第三份手抄清单（`service/backup.AllTables` 28 项 vs 注册表 37 张表 ⇒ 导出静默缺口 = 恢复后状态不一致，修复在飞）· `ApplyRuntimeSettings` 的破坏性静默丢弃（`payload_rules` / `openai_service_tier_rules`：一条坏行清空已配置规则）与两处无告警丢弃（`checkin_schedule_mode` / `notify_task_toggles`），修复在飞 · `POST /api/accounts/verify-token` 失败时只回 400 `token verification failed`、服务端零日志（实测：同一 token 手工调用 200、e2e 链 400，无日志可查根因）· `channel selection failed err=<nil>` 这类「说了但没说原因」的 WARN · 5 张高频写入表无保留期（`model_probe_results` ≈2.9 万行/天）· usage 聚合无纠偏路径 · 41 个 TS 默认时间戳列 + SQLite TEXT PK 可空 + `textPK()` 死码 + 可空带默认 bool 的拷贝覆盖显式 NULL · env parity 门禁缺「过期白名单」检查 · `sleepCtx` 两份副本与签到 per-account ctx 传播 · 配置面 fail-loud + 生效值回显（最贵路径：`DB_TYPE` typo 静默起空 SQLite）· 计费回执 + 票据后结算（503 不退账）· 前端幂等写 + 结果分类 · web 侦察 F5–F12 · `settings_apply.go` 24 处 `upsertSettingDB` 丢错误 · 视觉 P2×3 与术语统一批 · axonhub / gpt-load 平台适配器 · 以及原波次 3 未做项（expiresAt 存量归一、视觉基线龄期治理、downstream-keys count 视图、reset-usage UI 入口）。完整清单与验收见 [`progress/MASTER.md`](progress/MASTER.md)。|
| Local dev env      | **`.dev-local/`（gitignored）**：Linux 原生工具链——原生二进制 + 真实上游测试床（new-api / sub2api / PostgreSQL / Redis）驱动 `scripts/e2e/*` 真实平台链与双语视觉 e2e；手册见 `.dev-local/README.md` |
| Open work          | **#1035 结构性专题 S1–S10 全部收口（2026-08-30，issue 已关闭）**；F3 告警文案 i18n 已交付（#1091 共享 attention-label 模块，铃铛+面板双语端到端）；**F5 批次 2（alert 族事件结构化，批次 1 已随 #1128 交付）**、**后端英文文案本地化后续批次**（admin 认证节已启动，其余按 errorCode 递增迁移）、**术语统一批**（通道/渠道、账户/账号、密钥三写；建议配 i18n lint 门禁）、证据收口项（cascade 生产证明、#558 可选探针）见 [MASTER](progress/MASTER.md) |
| Stack              | Go 1.26.6 · React 19 + Bun + Rsbuild 2 + TanStack Router/Query/Table + Tailwind 4 + shadcn Base UI + OKLCH + i18next · dual dialect SQLite/PostgreSQL                                                                                                                                                                                                                                                                                                                                                                                          |
| Runtime shape      | single embedded SPA binary · **16** default background schedulers（update-center 仅显式启用时注册）· OAuth callback listeners start only with an active flow                                                                                                                                                                                                                                                                                                                                                                                   |
| Brand              | **Metapi** · transparent-base π glyph + blue-cyan gradient SVG `web/public/logo.svg` (single asset, light/dark readable) + `favicon.svg` · served from the embedded SPA root by the router whitelist                                                                                                                                                                                                                                                                                                                                                                          |

## Product honesty

| ID                        | Status                    | Note                                                                                                                                                                |
| :------------------------ | :------------------------ | :------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| cascade                   | **partial**               | HTTP load proof exists; production multi-channel proof still required                                                                                               |
| usage stats               | **present-with-residual** | media detail fold shipped; multi-instance lag remains                                                                                                               |
| Responses WebSocket       | **present**               | single-instance honesty; no cluster-wide sticky claim                                                                                                               |
| Redis sticky sessions     | **deferred**              | use a single instance or load-balancer pinning                                                                                                                      |
| Update-center deploy      | **external present**      | releases/GHCR are external; API residual remains 501                                                                                                                |
| PostgreSQL pool budget    | **present**               | profiles + lease backoff; deployment must still respect role LIMIT                                                                                                  |
| RE2 user-id extraction    | **present**               | pre-compiled RE2-safe regex with an 8-digit cap                                                                                                                     |
| OAuth token refresh       | **present**               | shared explicit account/site projection supports SQLite and PostgreSQL                                                                                              |
| UI i18n verification      | **present**               | en + zh-CN identical key sets through the vitest i18n gate; language switcher, browser-language detection, `documentElement.lang`/`dir`, and sidebar keys are wired |
| EN label coverage         | **present**               | user-facing UI copy uses i18next; locale parity and call-site scanning are test-gated                                                                               |
| Theme customization       | **present**               | 5 axes (preset/font/radius/scale/content-layout), 10 presets, direct `--chart-1…5` CSS variables in Recharts, cookie persistence; serif is explicit opt-in          |
| Daily metric truth        | **present**               | shared local-day aggregation, real reward/check-in, partial-truth metadata, fail-closed query errors, and per-account reward/spend status gates                     |
| Guided onboarding         | **present**               | site→account→route chain carries the `siteId`/`create` deep link, inline credential verification (session/API key), and channelDrafts handoff to the route form     |
| Route allocation/price    | **present**               | route detail shows configured weight + enabled share (disabled excluded) and normalized input/output price joined by concrete model + accountId, with provenance     |
| Tester comparison truth  | **present**               | synchronous forced-channel probes preserve harness/upstream status, latency, errors, bounded concurrency, abort state, and exclude disabled channels                  |
| URL table state           | **present**               | list-page pagination, sorting, filtering, and search have one URL owner with stable callbacks and Chromium smoke coverage                                          |
| Windows local development | **loopback by default**   | empty `HOST` binds `127.0.0.1`; containers/server platforms explicitly retain `0.0.0.0`                                                                             |
| Resin per-site sticky     | **present**               | per-site tri-state override of `resin_enabled`/`use_utls` (inherit / force-on / force-off) writes through CreateSite/UpdateSite; settings hydration symmetric across restarts |
| Dialog viewport contract  | **present**               | `DialogContent`/`AlertDialogContent`/`PopoverContent` carry `max-h` + `overflow-y-auto`; `DialogHeader` sticky top, `DialogFooter`/`AlertDialogFooter` opaque sticky bottom; `dialog-viewport.test.ts` static guard prevents translucency regression |
| PG BOOLEAN dialect gate   | **present**               | `docs/pg_boolean_gate_test.go` statically bans `COALESCE(<bool>, 0)` and `<bool> = [01]` across all 16 known BOOLEAN columns; complements `pg_rebind_gate_test.go` (42601) by covering 42804 |
| Visual regression gate    | **present**               | CI `visual-regression` job: 10 key pages (dashboard/token-routes/accounts/sites/models/channels/oauth/model-tester/settings×2) light-desktop golden diff, maxDiffPixelRatio 0.01, baselines in `web/visual-baselines/`; plus `ui-screenshots` evidence pipeline (full-route capture → artifact) |
| Theme contrast gate       | **present**               | `contrast-gate.test.ts` parses both theme CSS levels (incl. color-mix cascade) and asserts 320 token pairs ≥ 4.5:1; 0 known residuals (exemption table cleared; 10 presets × light/dark all pass AA) |
| CI upstream coverage      | **4/16 live**             | test-e2e runs real container chains against new-api/one-api (smoke.sh) + sub2api/cliproxyapi (verify-token-import.sh); the other 12 adapters are fixture-tested, live probes are operator-gated |
| Runtime platform matrix   | **4/5 smoke**             | `runtime-smoke-matrix` job: ubuntu/macos/windows/arm64 native build + start + /ready + /v1 smoke on every PR; release binaries still build 5 platforms (linux/darwin/windows × amd64/arm64) with linux-amd64 smoke |

## Current pointers

- Open work and three delivery mainlines: [`progress/MASTER.md`](progress/MASTER.md)
- As-built package and request flow: [`architecture.md`](../architecture.md)
- Public deployment variables and upgrade procedure: [`deployment.md`](../deployment.md)

## Branch hygiene

| Fact              | Value                                                                                                               |
| :---------------- | :------------------------------------------------------------------------------------------------------------------ |
| Model             | GitHub Flow — master 唯一长期分支（受保护），短命分支（`fix/*`/`feature/*`/`chore/*`/`docs/*`）→ PR → Squash merge  |
| Default branch    | `master`                                                                                                            |
| Master protection | PR required + 12 CI status checks required + enforce admins; no approve requirement; squash-only merge (repo-level) |
| Workflow doc      | [`git-workflow.md`](git-workflow.md)                                                                                |
