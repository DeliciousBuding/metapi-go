#!/bin/sh
# Standalone compatibility entrypoint for contributors who set
# core.hooksPath=.githooks. The managed global hook-kit calls
# pre-push-project directly, so the project gate has one implementation.
exec "$(dirname "$0")/pre-push-project" "$@"
