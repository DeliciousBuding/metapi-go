# Frontend source dependencies (not needed for Go build)
web/node_modules/
web/build/
web/docs/.vitepress/dist/

# Frontend build output (built in CI/Docker, embedded via go:embed)
web/dist/

# Accidental root npm (real package.json lives under web/)
/node_modules/
/package.json
/package-lock.json
/test-results/

# Binaries
/bin/
/dist/
/metapi
/metapi-go
/metapi.exe
/metapi-go.exe
/server
*.exe

# Certificates & private keys (never commit)
*.pem
*.key
*.crt
*.p12

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.local
*.env

# Data
/data/
*.db
*.sqlite

# Golden TS fixture for the migration-compat tests — intentionally tracked
# (dummy data, built by scripts/regen-ts-fixture.sh).
!store/testdata/ts-source/hub.db

# Build
/tmp/
metapi.exe
metapi-migrate.exe
migrate.exe
server.exe
routing.test.exe
*.test.exe

# Electron desktop shell artifacts (electron/ is a separate Node project)
electron/node_modules/
electron/dist/
electron/metapi
electron/metapi.exe
electron/*.log

# Test artifacts
coverage.out
*.out

# E2E runtime data
/e2e/data/

# Local smoke-test artifacts
/tmp-smoke/

# Playwright artifacts (web/)
web/test-results/
web/playwright-report/
web/blob-report/
web/playwright/.cache/

# Agent worktrees
.worktrees/
.claude/

# Agent handoff files (never commit these)
/BLOCKED.md
/PROGRESS.md
