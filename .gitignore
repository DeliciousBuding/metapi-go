# Frontend source dependencies (not needed for Go build)
web/node_modules/
web/build/

# Frontend build output (built in CI/Docker, embedded via go:embed)
web/dist/

# Accidental root npm (real package.json lives under web/)
/node_modules/
/package.json
/package-lock.json
/bun.lock
/test-results/

# Binaries
/bin/
/dist/
/metapi
/metapi-go
/metapi.exe
/metapi-go.exe
/server
*.exe

# Certificates & private keys (never commit)
*.pem
*.key
*.crt
*.p12

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.local
*.env
.env.*
!.env.example

# Data
/data/
*.db
*.sqlite

# Logs (only electron/*.log was covered before)
*.log

# Release staging (scripts/release.sh + the release workflow build here)
/dist-bin/

# Bun binary lockfile (the repo tracks the text web/bun.lock)
*.lockb

# Golden TS fixture for the migration-compat tests — intentionally tracked
# (dummy data, built by scripts/regen-ts-fixture.sh).
!store/testdata/ts-source/hub.db
# SQLite WAL sidecars materialise next to the golden fixture whenever something
# opens it in place (sqlite3 CLI, an ad-hoc query). The fixture bytes never
# change; keep the sidecars out of `git status`.
store/testdata/ts-source/hub.db-shm
store/testdata/ts-source/hub.db-wal

# Build
/tmp/
metapi.exe
metapi-migrate.exe
migrate.exe
server.exe
routing.test.exe
*.test.exe

# Test artifacts
coverage.out
*.out

# E2E runtime data
/e2e/data/

# Local smoke-test artifacts
/tmp-smoke/

# Playwright artifacts (web/)
web/test-results/
web/playwright-report/
web/blob-report/
web/playwright/.cache/

# Agent worktrees
.worktrees/
.claude/

# Agent handoff files (never commit these)
/BLOCKED.md
/PROGRESS.md

# Local repro/scratch artifacts (never commit)
.repro/

# local dev instance matrix (Wave 7 local toolchain)
.dev-local/
