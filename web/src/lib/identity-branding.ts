// metapi-go/lib — identity branding for the Metapi product.
// Brand name + labels. The logo mark is components/ui/brand-logo.tsx (inline
// SVG whose gradient follows the active preset's --primary); static file
// assets (public/logo.svg, favicon) exist for browser chrome and external use.

export const metapiIdentity = {
  name: 'Metapi',
  continueLabelKey: 'Continue with Metapi',
} as const
